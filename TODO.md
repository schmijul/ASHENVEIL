# Ashenveil TODO

## Opening Loop Integration

- Fix melee hit registration so `CombatAttack_NearbyBoar_ReducesWildlifeHealth` passes in PlayMode.
- Resolve the repeated `StorageBarn/collider4` negative `BoxCollider` warning emitted when `Grauwald` loads.
- Re-run Unity batch tests and keep the branch gated on green EditMode and PlayMode runs.

## After Opening Loop Is Green

- Replace runtime placeholder NPCs, loot, and wildlife setup with authored scene content or prefabs.
- Decide whether to continue with `#8 Trade System` or do another integration pass for the first five minutes of the demo.
- Reconcile the stale `docs/BUILD_LOG.md` status entries with actual git history before the next merge train.
